[providers]
python = "3.11"

[build]
cmds = [
  "cd farm-sx && pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "cd farm-sx/backend && uvicorn src.api.main:app --host 0.0.0.0 --port 8000"
